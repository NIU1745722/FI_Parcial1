#include <iostream>
/*
using namespace std;
#define DIM 5

void LlegirVector(int vec[], int dim) {
	int num;
	for (int i = 0; i < dim; i++) {
		cin >> num;
		vec[i] = num;
	}
}

void EscriuVector(int vec[], int dim) {
	for (int i = 0; i < dim;  i++) {
		cout << vec[i] << " ";
	}
}

void SumaVectors(int vec1[], int vec2[], int vecSum[], int dim) {
	int num;
	for (int i = 0; i < dim; i++) {
		vecSum[i] = vec1[i] + vec2[i];
	}
}

int main() {
	int vec1[DIM], vec2[DIM], vecSum[DIM];
	LlegirVector(vec1, DIM);
	LlegirVector(vec2, DIM);
	SumaVectors(vec1, vec2, vecSum, DIM);
	EscriuVector(vecSum, DIM);
}
*/