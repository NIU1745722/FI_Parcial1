/*
#include <iostream>

using namespace std;

int main()
{
	string acta;
	float teo, sem, prac, nota;
	cout << "Dame notas: ";
	cin >> teo >> sem >> prac;
	nota = 0.4 * teo + 0.3 * sem + 0.3 * prac;
	if (nota < 5) {
		acta = "SUSPES";
	}
	else if (nota < 7) {
		acta = "APROVAT";
	}
	else if (nota < 9) {
		acta = "NOTABLE";
	}
	else if (nota < 10) {
		acta = "EXCEL.LENT";
	}
	else {
		acta = "MATRICULA D’HONOR";
	}
	cout << "La nota final es " << nota << " - " << acta;
}
*/