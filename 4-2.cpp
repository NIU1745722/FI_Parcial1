/*
#include <iostream>
using namespace std;

int numRand(int min, int max) {
	int num;
	num = (rand() % (max - min + 1)) + min;
	return num;
}

int main() {
	int min, max, random;
	cin >> min >> max;
	random = numRand(min, max);
	cout << random;
}
*/