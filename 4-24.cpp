/*
#include <iostream>
#include <math.h>
using namespace std;

void polars(float x, float y, float& modul, float& angle) {
	float raizX, raizY;
	raizX = x * x;
	raizY = y * y;
	modul = sqrt(raizX + raizY);
	angle = atan(y / x);
}
*/