/*
#include <iostream>
using namespace std;
int main()
{
    int x, y, z;
    bool res1, res2, res3;
    cout << "Introdueix 3 nombres enters: ";
    cin >> x;
    cin >> y;
    cin >> z;
    res1 = (x < 7) && ((y > z) || (7 > z));
    res2 = ((x == 'c') && (y < -5)) && ((z >= 100) || (z < 6));
    res3 = ((9 >= x) && (13 < y)) || (-36 >= z);
    cout << "Resultat de les expressions: " << res1 << " " << res2 << " " << res3;
}
*/