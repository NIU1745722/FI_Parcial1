/*
#include <iostream>
using namespace std;
int main()
{
    int x, y;
    bool res;
    cout << "Introduix valors x, y: ";
    cin >> x;
    cin >> y;
    res = (x>= 0 && y>= 0) && (x <= 10 && y <= 10);
    cout << "El resultat es: " << res;
}
*/